# DashScope Go SDK

API reference aligned with dashscope-sdk-python.

```{toctree}
:maxdepth: 2

README.md
develop.md
```

Packages: `dashscope`, `aigc`, `embeddings`, `rerank`, `app`, `audio`, `nlp`, `assistants`, `threads`, `finetune`, `tokenizers`, `agentstudio`, `multimodal`.
